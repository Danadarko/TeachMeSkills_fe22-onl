export enum AppPages {
    LOGIN = "/login",
    REGISTRATION = "/registration",
    LANDING = "/landing",
    POSTS = "/posts",
    
    RESET_PASSWORD = "/reset-password",
    SUCCESS_PAGE = "/success-page",
  }